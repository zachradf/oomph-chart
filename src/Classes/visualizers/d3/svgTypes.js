export default class SVGTypes {
  constructor() {
    const types = {
      bar: {
        _selfKey: 'bar',
        legacyName: 'BAR',
        legacyValue: 'bar-chart',
      },
    };

    Object.assign(this, types);
  }
}
